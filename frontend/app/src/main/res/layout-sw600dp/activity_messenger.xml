<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <include layout="@layout/component_top_bar" android:id="@+id/top_bar" />

    <androidx.slidingpanelayout.widget.SlidingPaneLayout
        android:id="@+id/sliding_pane"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Left pane: 320dp room/contact list -->
        <LinearLayout
            android:layout_width="320dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/surface"
            android:elevation="2dp">

            <EditText
                android:id="@+id/et_search"
                android:layout_width="match_parent"
                android:layout_height="@dimen/height_input"
                android:hint="Tìm kiếm..."
                android:paddingHorizontal="@dimen/spacing_md"
                android:layout_margin="@dimen/spacing_md"
                android:background="@drawable/bg_input"
                android:inputType="text"
                android:maxLines="1"
                style="@style/TextAppearance.Cafe.Body" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingHorizontal="@dimen/spacing_md"
                android:paddingBottom="@dimen/spacing_sm">

                <Button
                    android:id="@+id/btn_tab_rooms"
                    android:layout_width="0dp"
                    android:layout_height="@dimen/height_button"
                    android:layout_weight="1"
                    android:layout_marginEnd="@dimen/spacing_xs"
                    android:text="Phòng"
                    style="@style/Widget.Cafe.Button.Primary" />

                <Button
                    android:id="@+id/btn_tab_contacts"
                    android:layout_width="0dp"
                    android:layout_height="@dimen/height_button"
                    android:layout_weight="1"
                    android:layout_marginStart="@dimen/spacing_xs"
                    android:text="Danh bạ"
                    style="@style/Widget.Cafe.Button.Secondary" />
            </LinearLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_rooms"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_contacts"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/tv_empty"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Không có dữ liệu"
                    android:gravity="center"
                    android:layout_gravity="center"
                    style="@style/TextAppearance.Cafe.Body"
                    android:textColor="@color/text_soft"
                    android:visibility="gone" />

                <ProgressBar
                    android:id="@+id/progressLoading"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:visibility="gone" />
            </FrameLayout>
        </LinearLayout>

        <!-- Right pane: messaging details -->
        <LinearLayout
            android:id="@+id/container_message_pane"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:background="@color/bg_primary">

            <!-- Selection helper -->
            <TextView
                android:id="@+id/tv_select_room_hint"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="Chọn một phòng để bắt đầu"
                android:gravity="center"
                style="@style/TextAppearance.Cafe.Body"
                android:textColor="@color/text_soft"
                android:visibility="visible" />

            <!-- Active Conversation layout -->
            <LinearLayout
                android:id="@+id/layout_chat_area"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:visibility="gone">

                <!-- Header for room name -->
                <TextView
                    android:id="@+id/tv_active_room_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="@dimen/spacing_md"
                    android:background="@color/surface"
                    style="@style/TextAppearance.Cafe.Title"
                    android:gravity="center"
                    android:elevation="2dp" />

                <!-- Messages Frame -->
                <FrameLayout
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1">

                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/rv_chat_messages"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipToPadding="false"
                        android:paddingVertical="@dimen/spacing_md" />

                    <include
                        android:id="@+id/right_pane_empty_state"
                        layout="@layout/component_empty_state"
                        android:visibility="gone"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:layout_gravity="center" />
                </FrameLayout>

                <!-- Input actions -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/surface"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="@dimen/spacing_md"
                    android:elevation="4dp">

                    <EditText
                        android:id="@+id/et_message_input"
                        android:layout_width="0dp"
                        android:layout_height="@dimen/height_input"
                        android:layout_weight="1"
                        android:background="@drawable/bg_input"
                        android:paddingHorizontal="@dimen/spacing_md"
                        android:inputType="textShortMessage"
                        android:imeOptions="actionSend"
                        style="@style/TextAppearance.Cafe.Body"
                        android:hint="Nhập tin nhắn..."
                        android:layout_marginEnd="@dimen/spacing_sm" />

                    <Button
                        android:id="@+id/btn_message_send"
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/height_button"
                        style="@style/Widget.Cafe.Button.Primary"
                        android:text="Gửi" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
    </androidx.slidingpanelayout.widget.SlidingPaneLayout>
</LinearLayout>
